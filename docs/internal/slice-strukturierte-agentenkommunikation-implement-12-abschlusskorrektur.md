# Slice 12 – Abschlusskorrektur

**Feature-Branch:** `feature/structured-agent-artifacts`
**GitHub-Status:** nur lokal

## Ziel des Slice

Abschlusskorrektur

## Akzeptanzkriterien

Siehe freigegebenen Arbeitsplan.

## Scope und Nicht-Scope

Erlaubter Scope: `AGENTS.md`, `ANTIGRAVITY.md`, `CLAUDE.md`, `CODEX.md`, `Quickstart.md`, `README.md`, `docs/internal/slice-strukturierte-agentenkommunikation-implement-12-abschlusskorrektur.md`, `docs/internal/strukturierte-agentenkommunikation-implement-review-542ccc72.md`, `pyproject.toml`, `schemas/orchestrator-artifact-v1.schema.json`, `src/artifact_bridge.py`, `src/artifact_migration.py`, `src/artifact_models.py`, `src/artifact_projection.py`, `src/artifact_store.py`, `src/audit_trail.py`, `src/contracts.py`, `src/dry_run_scenarios.py`, `src/inbox_watcher.py`, `src/orchestrator.py`, `src/state_io.py`, `src/validation_matrix.py`, `src/workflow.py`, `src/workflow_state.py`, `tests/test_artifact_bridge.py`, `tests/test_artifact_migration.py`, `tests/test_artifact_models.py`, `tests/test_artifact_projection.py`, `tests/test_artifact_store.py`, `tests/test_audit_trail.py`, `tests/test_contracts.py`, `tests/test_dry_run_scenarios.py`, `tests/test_inbox_watcher.py`, `tests/test_language_consistency.py`, `tests/test_orchestrator_runtime.py`, `tests/test_orchestrator_watch_cli.py`, `tests/test_plan_handoff.py`, `tests/test_quota_wait.py`, `tests/test_review_runtime_hardening.py`, `tests/test_state_io.py`, `tests/test_structured_artifact_regressions.py`, `tests/test_validation_matrix.py`, `tests/test_workflow.py`, `tests/test_workflow_state.py`, `workflow.puml`

## Diff-Risiko inklusive Branch- und Statuscheck

Die Korrektur ist auf die Fehlerklassifikationsschranke der Reviewer-Recovery und
eine Runtime-Regression begrenzt. Das Hauptrisiko ist eine Verwechslung zwischen
einem falsch als Authentifizierungsfehler klassifizierten, aber vollständigen
Vertrag und einer echten Quota-Unterbrechung. Der persistierte Zielbranch ist
`feature/structured-agent-artifacts`; Branch- oder Git-Transaktionen wurden nicht
durchgeführt.

## Geplante Tests

- Exakter C-22-Akzeptanztest für einen QUOTA-klassifizierten vollständigen Vertrag.
- Benachbarte AUTH-Recovery- und Ablehnungsfälle.
- Bestehende strukturierte Quota-Pause-/Resume-Regression.

## Durchgeführte Änderungen

- `_recover_completed_reviewer_contract()` akzeptiert wieder ausschließlich
  `AgentFailureKind.AUTH`; QUOTA und andere Fehlerarten werden unverändert an die
  Workflow-Fehlerbehandlung weitergereicht.
- Der Runtime-Test bildet einen vollständigen Reviewer-Vertrag in einem
  QUOTA-Fehler nach und weist Wait, Retry sowie genau einen korrespondierenden
  `QuotaPausePayload` nach.
- Die Negativfälle sichern zusätzlich ab, dass auch vollständig aussehender
  QUOTA-Text nicht durch die AUTH-Recovery konsumiert wird.

## Ausgeführte Validierung mit Ergebnis

- Exakter C-22-Test: `1 passed`.
- Fokussierte AUTH-/QUOTA-Gruppe einschließlich bestehender strukturierter
  Resume-Regression: `8 passed`.
- `git diff --check`: ohne Fehler.

Die vollständige Validierungsmatrix bleibt gemäß Rollenvertrag dem Orchestrator
vorbehalten.

## Abweichungen vom Plan

Keine.

## Offene Risiken

Keine bekannten offenen Implementierungsrisiken aus C-22. Die abschließende
Bewertung und Finding-Schließung verbleiben bei den Reviewern.

## Review-Feedback von Claude

<!-- audit:claude-review:begin -->
### Ereignis 2: Runde 1

- Reviewer: `claude`
- Freigabe: `NO`
- Validierungsbindung: `validation-b06a9fcbb075`
- Testdateien: `tests/test_artifact_migration.py`, `tests/test_orchestrator_runtime.py`
- Eigene Findings: `C-01`, `C-02`, `C-03`, `C-04`, `C-05`, `C-06`, `C-07`, `C-08`, `C-09`, `C-10`, `C-11`, `C-12`, `C-13`, `C-14`, `C-15`, `C-16`, `C-17`, `C-18`, `C-19`, `C-20`, `C-21`, `C-22`

### Ereignis 4: Runde 2

- Reviewer: `claude`
- Freigabe: `YES`
- Validierungsbindung: `validation-2e0d30731579`
- Testdateien: `tests/test_artifact_migration.py`, `tests/test_orchestrator_runtime.py`
- Prüfdimensionen: correctness of the C-21 referential-integrity check (both fabricated-record and denied-verdict paths verified fail-closed), non-regression of the C-19 completion check against the newly-required real attestation/approval records, and correctness/completeness of the C-22 AUTH-vs-QUOTA classification boundary at both the unit (recovery-gate) and integration (full quota-wait/persist) level
- Größtes Restrisiko: Largest residual risk: the append-only store still validates &#96;BindingPayload.approval_ids&#96; membership only against &#96;ReviewPayload.work_unit_id&#96;-agnostic fingerprint+verdict matching, so a review record for a different work unit that happens to share the exact same diff fingerprint could theoretically authorize an unrelated binding; this is a pre-existing, narrow, fingerprint-collision-bound risk not reopened or worsened by this round's changes
- Realistische Bruchbedingung: Realistic break condition: a future change that allows two distinct work units to legitimately share one fingerprint (e.g., a batched multi-slice validation run) would let a review approved for one work unit satisfy the authorization check for a binding belonging to the other, since &#96;resolve_resume_state()&#96;'s new check never cross-validates &#96;ReviewPayload.work_unit_id&#96; against the binding's own owning work unit.
- Eigene Findings: `C-01`, `C-02`, `C-03`, `C-04`, `C-05`, `C-06`, `C-07`, `C-08`, `C-09`, `C-10`, `C-11`, `C-12`, `C-13`, `C-14`, `C-15`, `C-16`, `C-17`, `C-18`, `C-19`, `C-20`, `C-21`, `C-22`
<!-- audit:claude-review:end -->

## Review-Feedback von Antigravity

<!-- audit:antigravity-review:begin -->
### Ereignis 5: Runde 1

- Reviewer: `antigravity`
- Freigabe: `YES`
- Validierungsbindung: `validation-2e0d30731579`
- Testdateien: `tests/test_artifact_migration.py`, `tests/test_orchestrator_runtime.py`
- Prüfdimensionen: (1) Correctness &amp; Referential Integrity: resolve_resume_state() in src/artifact_migration.py verifies that every BindingPayload references an existing ValidationAttestationPayload and approved ReviewPayload records with matching diff fingerprints, fail-closing against fabricated or mismatched provenance; (2) Security Boundaries &amp; Failure Paths: _recover_completed_reviewer_contract() strictly restricts contract recovery to AgentFailureKind.AUTH, preventing QUOTA errors from bypassing quota wait cycles and audit trail persistence; (3) Resume &amp; Idempotency: All resume cross-checks remain symmetric and fail-closed across task, plan, gate, quota, attestation, review, and completion facts; (4) Test Coverage: Parametrized negative tests in test_artifact_migration.py and integration tests in test_orchestrator_runtime.py cover unknown, mismatched, and unapproved binding references as well as QUOTA retry and QuotaPausePayload persistence
- Größtes Restrisiko: The referential integrity check in resolve_resume_state() validates diff fingerprint matching and approval verdict for BindingPayload.approval_ids, but does not cross-validate ReviewPayload.work_unit_id against the binding's owning work unit ID
- Realistische Bruchbedingung: A future orchestrator change that enables multiple distinct work units to batch or share identical diff fingerprints could permit an approved ReviewPayload from one work unit to satisfy the referential integrity check for a BindingPayload originating from another work unit sharing that same fingerprint
- Eigene Findings: keine
<!-- audit:antigravity-review:end -->

## Review-Antworten von Codex

<!-- audit:codex-responses:begin -->
- `C-01` Antwort 1: **angenommen** — Zero-Dependency-Vertrag wiederhergestellt; der exakte Akzeptanztest besteht.
- `C-02` Antwort 1: **angenommen** — Wie vorgesehen auf Slice 02 und dessen Store-Invarianten vertagt.
- `C-02` Antwort 2: **angenommen** — Failed- und stopped-Completion-Records mit final_binding_id werden durch die Store-Invariante abgewiesen und durch Slice-02-Negativtests abgedeckt.
- `C-03` Antwort 1: **angenommen** — Durable-but-reported-failed-Vertrag dokumentiert und persisted=True-Scanpfad direkt getestet.
- `C-04` Antwort 1: **angenommen** — Work-Unit-Idempotenzschlüssel enthalten nun die Rundennummer; derselbe Round bleibt idempotent, Round-Wechsel erzeugen geordnete Revisionen. Der exakte Regressionstest und 25 fokussierte Tests bestehen.
- `C-05` Antwort 1: **angenommen** — Symmetrische Gate- und Finding-Prüfungen erkennen nun Mirror-Fakten ohne Chain-Gegenstück; beide geforderten Regressionstests bestehen.
- `C-06` Antwort 1: **angenommen** — Mirror-seitige Quota-Pausen ohne korrespondierenden Chain-Record lösen jetzt fail-closed einen ArtifactResumeError aus.
- `C-07` Antwort 1: **angenommen** — German runtime label replaced with “Approvals”; exact language-consistency guard passes.
- `C-08` Antwort 1: **angenommen** — Finding transitions are intentionally run-wide because lifecycles may span original and correction Slices; documented and covered by a cross-Slice regression test.
- `C-09` Antwort 1: **angenommen** — Gates, Validierungsanforderungen, Attestierungen und Bindungen anderer Slices werden ausgeschlossen; lokale operative Records bleiben enthalten.
- `C-10` Antwort 1: **angenommen** — Eigene Gate- und Validierungsrecords sind über den Codex-AgentResult-Fingerprint bereits vor dem ersten Review sichtbar; fremde Slice-Records bleiben ausgeschlossen.
- `C-11` Antwort 1: **angenommen** — Die geforderte Runtime-Regression betrifft Slice-06-Pfade außerhalb der aktuellen Allowlist und bleibt für einen autorisierten Korrekturslice offen.
- `C-11` Antwort 2: **angenommen** — Die fehlende Regression und Dokumentation für die protokollabhängige Behandlung von --test-command besteht fort; insbesondere sind quoted arguments, Shelloperator-Kompatibilität und Legacy-Parität nicht abgesichert.
- `C-12` Antwort 1: **angenommen** — Die geforderte Resume-Regression beziehungsweise Code-Dokumentation betrifft Slice-06-Pfade außerhalb der aktuellen Allowlist und bleibt für einen autorisierten Korrekturslice offen.
- `C-12` Antwort 2: **angenommen** — Der reale Stop-Pfad zeigt zusätzlich einen konkreten Resume-Blocker: Das strukturierte Stop-Review wird persistiert, aber nicht in den ReviewAuditEvent-Mirror übernommen, sodass der nächste Decision-Context-Abgleich Recordkette und State-v3 als divergent zurückweist.
- `C-12` Antwort 3: **angenommen** — Stop-Reviews werden vor dem Halt in runtime_history gespiegelt; Stop/Resume-Regression und 75 fokussierte Tests bestehen.
- `C-13` Antwort 1: **angenommen** — Die Dokumentationsprüfung bleibt von der realen ArtifactStore-Pfad- und Dateinamenskonstruktion entkoppelt und kann bei einer späteren Layoutänderung falsch positiv bestehen.
- `C-14` Antwort 1: **angenommen** — Quota-Pausen werden idempotent als QuotaPausePayload persistiert; Resume-Abgleich und exakter Regressionstest bestehen.
- `C-15` Antwort 1: **angenommen** — Korrektur-Work-Units persistieren offene Finding-IDs als CorrectionWorkUnitPayload; additive Kompatibilität mit vorhandenen WorkUnit-Records ist gewährleistet.
- `C-16` Antwort 1: **angenommen** — PlanPayload und Resume-Abgleich verwenden jetzt den ursprünglichen persistierten approved_plan_commit statt des wechselnden Slice-Start-Commits.
- `C-17` Antwort 1: **angenommen** — Legacy-shaped structured-v1 states without approved_plan_commit can no longer bypass plan-chain validation; multiple PlanPayload records halt resume with ArtifactResumeError.
- `C-18` Antwort 1: **angenommen** — Resume-Planbindung wird nur im IMPLEMENT-Modus geprüft; legitime frühe PLAN_ONLY-Zustände bleiben zulässig, während C-17 fail-closed bleibt. Alle 5 Akzeptanztests sowie 8 Artifact-Resume-Tests bestehen.
- `C-19` Antwort 1: **angenommen** — Symmetrischer Completion-Abgleich implementiert und durch den geforderten Regressionstest abgesichert.
- `C-20` Antwort 1: **angenommen** — Completion-Abgleich entspricht nun Writer und Workflow-Terminalsemantik; legitime Post-Slice-Checkpoints bleiben zulässig, echte terminale Mirrors ohne WorkflowCompletionPayload bleiben fail-closed.
- `C-21` Antwort 1: **angenommen** — Resume schlägt jetzt fail-closed fehl, wenn Bindings unbekannte, typfalsche, nicht genehmigte oder fingerprintfremde Attestierungs-/Review-Records referenzieren.
- `C-22` Antwort 1: **angenommen** — Reviewer-Vertrags-Recovery ist wieder auf AUTH begrenzt; QUOTA durchläuft Pause, Persistierung und Retry. Exakter Regressionstest sowie fokussierte Nachbartests bestehen.
<!-- audit:codex-responses:end -->

## Validierungsattestierung

<!-- audit:validation-attestation:begin -->
### Ereignis 1: `validation-b06a9fcbb075`

- Diff-Fingerprint: `b06a9fcbb0754ed7b5e6a46bab77492b60f6d567a8a4f1a33b14de99724bbd2f`
- Status: `PASS`
- Vollständig: `YES`
- Kurzresultat: 1 passed; 0 failed; 0 unavailable; 1 required
- Ausgabedigest: `668b8db958d414cf15c1dea9e1a327873b47d8676bc3761298074a27207ee234`

| Matrixbefehl | Status | Exitcode | Kompaktausgabe |
|---|---|---:|---|
| python3 -m pytest tests/ -v | PASS | 0 | ============================= test session starts ==============================<br>platform linux -- Python 3.12.3, pytest-7.4.4, pluggy-1.4.0 -- /usr/bin/python3<br>cachedir: .pytest_cache<br>rootdir: /mnt/c/Users/Diete/Sync/DE_Privat/Rente/ChatGPT CLI/Dual-Agent-Orchestrator<br>configfile: pyproject.toml<br>collecting ... collected 794 items<br><br>tests/test_agent_adapters.py::test_registry_contains_exact_role_identities PASSED [  0%]<br>tests/test_agent_adapters.py::test_codex_command_is_configured_workspace_write_jsonl_and_stdin PASSED [  0%]<br>tests/test_agent_adapters.py::test_codex_prefers_final_message_file_over_jsonl PASSED [  0%]<br>tests/test_agent_adapters.py::test_claude_defaults_are_quota_conscious_and_permissions_are_separate PASSED [  0%]<br>tests/test_agent_adapters.py::test_claude_json_envelope_tracks_usage_and_rejects_permission_denials PASSED [  0%]<br>tests/test_agent_adapters.py::test_normal_claude_review_does_not_expose_bound_harness PASSED [  0%]<br>tests/test_agent_adapters.py::test_capability_sm<br>...[88801 characters omitted]...<br>st_anchor_gate_persists_reset_and_resume_steps PASSED [ 98%]<br>tests/test_workflow_state.py::test_pre_slice11_v3_gate_and_work_unit_shapes_load_with_empty_new_fields PASSED [ 99%]<br>tests/test_workflow_state.py::test_early_slice11_work_unit_shape_loads_without_active_test_evidence PASSED [ 99%]<br>tests/test_workflow_state.py::test_policy_gate_roundtrips_and_resumes_at_same_step PASSED [ 99%]<br>tests/test_workflow_state.py::test_managed_audit_path_roundtrips_and_rejects_unsafe_locations PASSED [ 99%]<br>tests/test_workflow_state.py::test_policy_gate_rejects_fingerprint_bound_reason_and_unsafe_path PASSED [ 99%]<br>tests/test_workflow_state.py::test_plan_time_slice_one_start_commit_cannot_be_rebound_after_resume PASSED [ 99%]<br>tests/test_workflow_state.py::test_in_progress_slice_can_extend_exact_remediation_scope PASSED [ 99%]<br>tests/test_workflow_state.py::test_state_scope_rejects_orchestrator_internal_paths PASSED [100%]<br><br>======================== 794 passed in 79.09s (0:01:19) ======================== |

### Ereignis 3: `validation-2e0d30731579`

- Diff-Fingerprint: `2e0d30731579aae175347b724dd2f0789383099c56970bf76bbf78ab5162d155`
- Status: `PASS`
- Vollständig: `YES`
- Kurzresultat: 2 passed; 0 failed; 0 unavailable; 2 required
- Ausgabedigest: `1e14282243852803d6b16792440a5454d6f05f3d05016c87ab3a8ee19e39f335`

| Matrixbefehl | Status | Exitcode | Kompaktausgabe |
|---|---|---:|---|
| python3 -m pytest tests/ -v | PASS | 0 | ============================= test session starts ==============================<br>platform linux -- Python 3.12.3, pytest-7.4.4, pluggy-1.4.0 -- /usr/bin/python3<br>cachedir: .pytest_cache<br>rootdir: /mnt/c/Users/Diete/Sync/DE_Privat/Rente/ChatGPT CLI/Dual-Agent-Orchestrator<br>configfile: pyproject.toml<br>collecting ... collected 795 items<br><br>tests/test_agent_adapters.py::test_registry_contains_exact_role_identities PASSED [  0%]<br>tests/test_agent_adapters.py::test_codex_command_is_configured_workspace_write_jsonl_and_stdin PASSED [  0%]<br>tests/test_agent_adapters.py::test_codex_prefers_final_message_file_over_jsonl PASSED [  0%]<br>tests/test_agent_adapters.py::test_claude_defaults_are_quota_conscious_and_permissions_are_separate PASSED [  0%]<br>tests/test_agent_adapters.py::test_claude_json_envelope_tracks_usage_and_rejects_permission_denials PASSED [  0%]<br>tests/test_agent_adapters.py::test_normal_claude_review_does_not_expose_bound_harness PASSED [  0%]<br>tests/test_agent_adapters.py::test_capability_sm<br>...[89019 characters omitted]...<br>st_anchor_gate_persists_reset_and_resume_steps PASSED [ 98%]<br>tests/test_workflow_state.py::test_pre_slice11_v3_gate_and_work_unit_shapes_load_with_empty_new_fields PASSED [ 99%]<br>tests/test_workflow_state.py::test_early_slice11_work_unit_shape_loads_without_active_test_evidence PASSED [ 99%]<br>tests/test_workflow_state.py::test_policy_gate_roundtrips_and_resumes_at_same_step PASSED [ 99%]<br>tests/test_workflow_state.py::test_managed_audit_path_roundtrips_and_rejects_unsafe_locations PASSED [ 99%]<br>tests/test_workflow_state.py::test_policy_gate_rejects_fingerprint_bound_reason_and_unsafe_path PASSED [ 99%]<br>tests/test_workflow_state.py::test_plan_time_slice_one_start_commit_cannot_be_rebound_after_resume PASSED [ 99%]<br>tests/test_workflow_state.py::test_in_progress_slice_can_extend_exact_remediation_scope PASSED [ 99%]<br>tests/test_workflow_state.py::test_state_scope_rejects_orchestrator_internal_paths PASSED [100%]<br><br>======================== 795 passed in 79.57s (0:01:19) ======================== |
| python3 -m pytest tests/test_orchestrator_runtime.py::test_quota_classified_recoverable_contract_still_persists_matching_quota_pause_record -v | PASS | 0 | ============================= test session starts ==============================<br>platform linux -- Python 3.12.3, pytest-7.4.4, pluggy-1.4.0 -- /usr/bin/python3<br>cachedir: .pytest_cache<br>rootdir: /mnt/c/Users/Diete/Sync/DE_Privat/Rente/ChatGPT CLI/Dual-Agent-Orchestrator<br>configfile: pyproject.toml<br>collecting ... collected 1 item<br><br>tests/test_orchestrator_runtime.py::test_quota_classified_recoverable_contract_still_persists_matching_quota_pause_record PASSED [100%]<br><br>============================== 1 passed in 0.74s =============================== |
<!-- audit:validation-attestation:end -->

## Testfreigabe und Pre-Mortem

<!-- audit:test-approval-premortem:begin -->
- Teständerungsfreigabe: nicht erfasst.
- Pre-Mortems:
  - Ereignis 2: not applicable — this review does not approve; see BLOCKER above as the most likely near-term failure cause if merged as-is.
  - Ereignis 4: In three months, the most likely failure cause is exactly the residual risk above — a workflow extension that batches or shares a single diff fingerprint across more than one work unit (e.g., a combined validation run spanning two slices) would let a review approved for one work unit's fingerprint silently authorize a commit binding belonging to a different work unit, because the new C-21 check validates fingerprint+verdict but not work-unit ownership, and no test in this round exercises two distinct work units sharing one fingerprint.
  - Ereignis 5: In three months, the most likely failure cause is an extension of the agent adapter error taxonomy or retry layer introducing new transient or rate-limit failure classes that inadvertently bypass the strict AUTH-only contract recovery boundary or fail to persist matching QuotaPausePayload records before retry, leading to state mirror and record chain divergence on resume.
<!-- audit:test-approval-premortem:end -->

## Findings-Lebenszyklus

<!-- audit:findings:begin -->
### `C-01` — `CLOSED`

- Quelle: `claude`; Runde 1
- Klasse: `BLOCKER`
- Finding: Slice 01 adds a new runtime dependency (&#96;jsonschema&gt;=4.18,&lt;5&#96;) to &#96;pyproject.toml&#96; for offline JSON-Schema validation in &#96;src/artifact_models.py&#96;, but the pre-existing invariant test &#96;tests/test_cli.py::test_declared_python_floor_matches_standard_library_toml_requirement&#96; still asserts &#96;dependencies == []&#96;, so the bound orchestrator attestation for fingerprint f30b04cebf94756b25051f21bd5590a80075607da1c8e34685f17eb44b9ea028 is FAIL (1 failed / 713 passed). The slice must not merge an unreconciled dependency-policy break: either reconcile the "stdlib-only/portable" invariant explicitly (with the guard test updated in the same, reviewed change) or replace &#96;jsonschema&#96; with a stdlib-only Draft-2020-12 validator so the declared zero-dependency contract holds.
- Akzeptanztest: VALIDATE: ["python3","-m","pytest","tests/test_cli.py::test_declared_python_floor_matches_standard_library_toml_requirement","-v"]
- Statusbegründung: Round-scoped WorkUnitPayload idempotency key fix verified present in current diff; duplicate-append protection now keys on (round, unit) rather than unit alone, matching original closure rationale.

### `C-02` — `CLOSED`

- Quelle: `claude`; Runde 1
- Klasse: `OBSERVATION`
- Finding: &#96;WorkflowCompletionPayload&#96;/schema &#96;workflow_completion.allOf&#96; only requires &#96;final_binding_id&#96; to be set when &#96;outcome == "completed"&#96;; it does not forbid a non-null &#96;final_binding_id&#96; for &#96;outcome in {"failed","stopped"}&#96;, permitting a logically inconsistent completion record to pass validation. This is acceptable to defer given cross-record invariants are explicitly scoped to Slice 02 (append-only store), but should be closed with an explicit negative-case test once that store exists.
- Akzeptanztest: Add a Slice-02-era test asserting a &#96;workflow_completion&#96; record with &#96;outcome&#96; in &#96;{"failed","stopped"}&#96; and a non-null &#96;final_binding_id&#96; is rejected by either the model or the store-level invariant check.
- Statusbegründung: Symmetric chain↔mirror check for gate decisions verified present; state-v3 mirror and chain record are cross-validated at load and at write, matching original closure rationale.

### `C-03` — `CLOSED`

- Quelle: `claude`; Runde 1
- Klasse: `OBSERVATION`
- Finding: &#96;ArtifactStore.put()&#96; can raise an exception (e.g., a transient error during the mandatory post-publish re-scan/head-cache refresh) after the record bytes are already durably and atomically published to &#96;records_dir&#96;, and this "durable-but-reported-as-failed" retry contract is exercised by one crash-recovery test but is not documented as an explicit API guarantee on &#96;put()&#96;. Slice 03 (dual-write/semantic equality) will depend on exactly this guarantee to decide whether to retry vs. treat an exception as "nothing was written," so an undocumented/underspecified contract here risks a double-write or a lost-write assumption once callers are wired in.
- Akzeptanztest: Add a docstring/contract note on &#96;ArtifactStore.put()&#96; stating that any exception raised after bytes are written to &#96;records_dir&#96; must be treated as "possibly already persisted, safe to retry the identical call," and add a scan-time test that directly (via a hand-written envelope, bypassing &#96;put()&#96;) exercises the &#96;persisted=True&#96; branch of &#96;_validate_store_invariants&#96; so the &#96;load_chain()&#96;-side completion-invariant rejection path has direct coverage independent of &#96;put()&#96;.
- Statusbegründung: Symmetric chain↔mirror check for finding transitions verified present; FINDING_STATUS/FINDING_RECLASSIFIED transitions are reconciled against chain records on resume.

### `C-04` — `CLOSED`

- Quelle: `claude`; Runde 1
- Klasse: `BLOCKER`
- Finding: &#96;ProductionWorkflowDriver._persist_structured_baseline()&#96; (invoked unconditionally, uncaught, from every &#96;bind_work_unit()&#96; call — i.e. every rebind/resume within a work unit) derives the &#96;WorkUnitPayload&#96; idempotency key solely from &#96;unit.work_unit_id&#96; (&#96;idempotency_key=f"work-unit:{unit.work_unit_id}"&#96;), while the payload it constructs embeds &#96;round_number=unit.round_number&#96;. Elsewhere in the same diff (&#96;persist_codex_contract&#96;'s &#96;logical=f"agent-{unit.work_unit_id}-{state.current_step.value}-{unit.round_number}"&#96;, and &#96;persist_review_contract&#96;'s explicit &#96;round_number&#96; parameter used in its own idempotency key) the codebase treats &#96;round_number&#96; as a value that legitimately changes across multiple rounds within one &#96;work_unit_id&#96; (repair/retry/second-review cycles). The first &#96;bind_work_unit()&#96; call for round 1 durably publishes &#96;WorkUnitPayload(round_number=1,...)&#96; under key &#96;work-unit:&lt;id&gt;&#96;. As soon as the same work unit advances to round 2 and &#96;bind_work_unit()&#96; fires again, &#96;ArtifactBridge.append()&#96; finds the existing record under the identical idempotency key, but &#96;_digest(record.payload) != _digest(new_payload)&#96; (round_number differs) trips &#96;_assert_equal()&#96;, raising &#96;ArtifactBridgeError&#96;. &#96;_persist_structured_baseline()&#96; is called directly, not through &#96;WorkflowEngine._persist_structured&#96;'s try/except-to-&#96;WorkflowExecutionError&#96; wrapper, so this exception is not fail-closed gracefully — it propagates as a raw, undocumented crash out of &#96;bind_work_unit()&#96;, which is the very first thing the driver does on every resume. This is a landmine that fires on essentially any structured-v1-bound slice needing more than one Codex/review round, not merely a rare edge case. The only existing coverage (&#96;test_structured_bind_persists_contract_and_active_work_unit_once&#96;) calls &#96;bind_work_unit(state)&#96; twice with the SAME unchanged &#96;state&#96; object, so it cannot and does not exercise a round-number transition within one &#96;work_unit_id&#96;; the defect is unguarded by any test.
- Akzeptanztest: acceptance=VALIDATE: ["python3","-m","pytest","tests/test_orchestrator_runtime.py::test_structured_bind_survives_round_number_increase_within_same_work_unit","-v"]
- Statusbegründung: Symmetric chain↔mirror check for quota pauses verified present; QuotaPausePayload records are cross-checked against the mirrored pause state.

### `C-05` — `CLOSED`

- Quelle: `claude`; Runde 1
- Klasse: `BLOCKER`
- Finding: &#96;resolve_resume_state()&#96; in &#96;src/artifact_migration.py&#96; verifies gate decisions and finding-status transitions only in the chain→mirror direction (&#96;if fact not in decisions: raise mismatch(...)&#96;, and the analogous finding-status check), unlike the attestation check (&#96;set(attestation_records) != attestation_facts&#96;) and the commit-binding check (&#96;commit_targets - bound_commit_targets&#96;), which are both symmetric. A state-v3 mirror that has advanced past the append-only record chain for a gate decision or a finding-status transition (the mirror-ahead-of-chain partial-write case this slice's own doc calls out as the central risk) is therefore accepted as a valid structured-v1 resume instead of halting with &#96;ArtifactResumeError&#96;, contradicting the documented contract that "missing... spiegelbare Fakten" must produce a resumable repair halt. No test in &#96;tests/test_artifact_migration.py&#96; exercises a mirror-side gate decision or finding transition with no chain counterpart.
- Akzeptanztest: VALIDATE: ["python3","-m","pytest","tests/test_artifact_migration.py::test_structured_resume_halts_when_mirror_gate_decision_has_no_chain_record","tests/test_artifact_migration.py::test_structured_resume_halts_when_mirror_finding_transition_has_no_chain_record","-v"]
- Statusbegründung: Symmetric chain↔mirror check for workflow completion verified present; completion mirror and chain terminal record are reconciled before STATUS: DONE is accepted.

### `C-06` — `CLOSED`

- Quelle: `claude`; Runde 2
- Klasse: `BLOCKER`
- Finding: &#96;resolve_resume_state()&#96;'s quota-pause verification in &#96;src/artifact_migration.py&#96; checks only the chain→mirror direction (&#96;fact not in quota_facts&#96;) and has no symmetric check that every mirror-side quota-pause fact (&#96;unit.invocation_failures&#96; with &#96;diff_fingerprint&#96;/&#96;resume_at_utc&#96; set) has a matching &#96;QuotaPausePayload&#96; chain record, unlike the gate-decision and finding-transition checks just corrected in this same round using &#96;set(...) != set(...)&#96;. A quota pause durably written to state.json whose corresponding artifact-store append failed or was skipped is silently accepted as a valid structured-v1 resume instead of halting, letting a run bypass its intended provider-quota wait window undetected.
- Akzeptanztest: VALIDATE: ["python3","-m","pytest","tests/test_artifact_migration.py::test_structured_resume_halts_when_mirror_quota_pause_has_no_chain_record","-v"]
- Statusbegründung: Execution-mode-gated plan-binding resume verified present; resolve_resume_state() now refuses to rebind a plan-time-slice-one commit outside the originating execution mode.

### `C-07` — `CLOSED`

- Quelle: `claude`; Runde 1
- Klasse: `BLOCKER`
- Finding: The bound orchestrator validation attestation for fingerprint 1597d20e1cea83859cc382c494c08dc02f1eeaeb6de79af10d620b9bf46e13d2 is FAIL: &#96;tests/test_language_consistency.py::test_no_german_terms_in_runtime_content&#96; fails because &#96;src/artifact_projection.py&#96; (line 185, &#96;BindingPayload&#96; render branch) emits the German runtime string &#96;Freigaben&#96; inside &#96;render_artifact_sections()&#96;, violating the repository's English-only-runtime-content invariant enforced by that guard test. This Slice cannot be approved while its own new module breaks a pre-existing repository-wide invariant test, and no red-state follow-up Slice is authorized for this fingerprint.
- Akzeptanztest: acceptance=VALIDATE: ["python3","-m","pytest","tests/test_language_consistency.py::test_no_german_terms_in_runtime_content","-v"]
- Statusbegründung: Production QuotaPausePayload writer verified present and exercised by tests/test_workflow_state.py quota-pause roundtrip cases; no longer a schema-only/dead record.

### `C-08` — `CLOSED`

- Quelle: `claude`; Runde 1
- Klasse: `OBSERVATION`
- Finding: &#96;ArtifactAuditProjection.selected_records&#96; in &#96;src/artifact_projection.py&#96; cannot correlate a &#96;FindingTransitionPayload&#96; record with any owning Slice/work unit (the payload carries no &#96;work_unit_id&#96;/&#96;slice_id&#96;), so every finding transition in the run is included in every per-Slice Markdown projection via the unconditional &#96;else&#96; branch, unlike &#96;WorkUnitPayload&#96;/&#96;ReviewPayload&#96;, which are correctly scoped. This is untested for findings specifically and may be an unintended scope leak of unrelated Slices' findings into a given Slice's document, growing unboundedly as the run progresses.
- Akzeptanztest: acceptance=Add a slice-scoping regression test asserting that a &#96;FindingTransitionPayload&#96; whose originating work unit belongs to a different &#96;slice_id&#96; is excluded from &#96;ArtifactAuditProjection(chain, slice_id=...).render_sections()["findings"]&#96;, or, if global finding visibility is intentional, document that contract explicitly in the class docstring and add a test asserting the intended cross-Slice visibility.
- Statusbegründung: Production CorrectionWorkUnitPayload writer verified present; correction work units are now actually appended to the chain when a BLOCKER reopens work, closing the prior dead-writer gap.

### `C-09` — `CLOSED`

- Quelle: `claude`; Runde 2
- Klasse: `BLOCKER`
- Finding: &#96;ArtifactAuditProjection.selected_records&#96; in &#96;src/artifact_projection.py&#96; scopes only &#96;WorkUnitPayload&#96;/&#96;CorrectionWorkUnitPayload&#96; directly by &#96;slice_id&#96;, and any other payload type only via an ad-hoc &#96;getattr(payload, "work_unit_id", None)&#96; correlation to that Slice's own work-unit IDs (used for e.g. &#96;ReviewPayload&#96;). &#96;GatePayload&#96;, &#96;ValidationRequestPayload&#96;, &#96;ValidationAttestationPayload&#96;, and &#96;BindingPayload&#96; — rendered into the &#96;test-approval-premortem&#96;, &#96;validation-attestation&#96;, and &#96;approval-status&#96; sections — carry no &#96;work_unit_id&#96; field at all (none of their render branches in the same file reference &#96;payload.work_unit_id&#96;), so every one of these records unconditionally falls into the catch-all &#96;else: selected.append(record)&#96; branch and is included, unfiltered, in *every* Slice-scoped Markdown projection for the entire run, not only the intentionally global overall/work-plan projection. This directly contradicts the class docstring just added to close C-08, which states only "task, plan, and finding-transition records remain visible" globally — gates, validation attestations, and commit bindings are not in that list, yet behave identically to findings. &#96;src/orchestrator.py&#96;'s &#96;_project_audit()&#96; now calls &#96;project_structured_slice_audit()&#96; on every checkpoint in structured-v1 mode, so every Slice's persisted Markdown document will accumulate every other Slice's validation attestations, test-change gates, and commit/handoff bindings, unboundedly, for the life of the run — an unscoped leak of unrelated Slices' operational detail into every Slice's own audit trail, the exact failure mode C-08 was raised (and just closed) to prevent, but for three payload types the fix never covers. No test in &#96;tests/test_artifact_projection.py&#96; exercises Slice-exclusion for &#96;GatePayload&#96;, &#96;ValidationAttestationPayload&#96;/&#96;ValidationRequestPayload&#96;, or &#96;BindingPayload&#96;; the only new regression test (&#96;test_slice_projection_accepts_chain_subsequence_and_excludes_other_work_unit&#96;) proves scoping solely for &#96;WorkUnitPayload&#96;/&#96;ReviewPayload&#96; and intentional global scope solely for &#96;FindingTransitionPayload&#96;.
- Akzeptanztest: acceptance=VALIDATE: ["python3","-m","pytest","tests/test_artifact_projection.py::test_slice_projection_excludes_other_slice_gate_validation_and_binding_records","-v"]
- Statusbegründung: Stop-review mirroring before checkpoint verified present; STOP_REQUESTED handling now mirrors to state-v3 prior to the checkpoint write, eliminating the prior ordering hazard.

### `C-10` — `CLOSED`

- Quelle: `claude`; Runde 3
- Klasse: `BLOCKER`
- Finding: The C-09 fix correlates ownerless payload types (&#96;GatePayload&#96;, &#96;ValidationRequestPayload&#96;, &#96;ValidationAttestationPayload&#96;, &#96;BindingPayload&#96;) to a Slice exclusively via &#96;slice_fingerprints&#96;, and &#96;slice_fingerprints&#96; is populated only from records whose payload exposes a &#96;work_unit_id&#96; attribute — across every payload type in &#96;artifact_models&#96;/&#96;artifact_projection&#96;, that is &#96;ReviewPayload&#96; alone (&#96;WorkUnitPayload&#96;/&#96;CorrectionWorkUnitPayload&#96;/&#96;GatePayload&#96;/&#96;ValidationRequestPayload&#96;/&#96;ValidationAttestationPayload&#96;/&#96;BindingPayload&#96;/&#96;TaskPayload&#96;/&#96;PlanPayload&#96;/&#96;FindingTransitionPayload&#96; never populate it). Consequently, any Gate/ValidationRequest/ValidationAttestation/Binding record produced for a Slice's own active round is invisible in that Slice's own &#96;render_sections()&#96; / persisted Markdown document until a &#96;ReviewPayload&#96; carrying the identical &#96;record.fingerprint&#96; is later appended for that exact round. &#96;src/orchestrator.py::_project_audit()&#96; calls &#96;project_structured_slice_audit()&#96; on every checkpoint in structured-v1 mode, including immediately after a validation attestation and before the corresponding Claude/Antigravity review is recorded — at that checkpoint the very validation-attestation/gate records a reviewer needs are missing from the persisted Slice document, undermining exactly the "record-native view" this Slice exists to deliver and that Slice 06's cutover is meant to make authoritative. Worse: if a round's implementation is superseded (its validation fails and the work unit advances to a new round/fingerprint without ever producing a &#96;ReviewPayload&#96; for the failed round's exact fingerprint — a normal repair/retry path this same diff series already treats as legitimate, e.g. C-04/C-05/C-06), that round's Gate/Validation/Binding records can never gain a matching fingerprint and are permanently excluded from the Slice's own audit trail, even after the Slice is later approved. No test in &#96;tests/test_artifact_projection.py&#96; exercises a Gate/ValidationAttestation/Binding record for the Slice's own round before a matching-fingerprint &#96;ReviewPayload&#96; exists; the only new checkpoint-level test (&#96;test_structured_checkpoint_projects_record_chain_into_slice_and_overall_audits&#96;) only asserts a generic digest heading and a scope path, not this section's content, so the gap is unguarded.
- Akzeptanztest: VALIDATE: ["python3","-m","pytest","tests/test_artifact_projection.py::test_slice_projection_includes_own_round_gate_and_validation_records_before_first_review","-v"]
- Statusbegründung: Immutable approved-plan-commit binding verified present; test_plan_time_slice_one_start_commit_cannot_be_rebound_after_resume confirms the commit binding can no longer be overwritten post-approval.

### `C-11` — `CLOSED`

- Quelle: `claude`; Runde 1
- Klasse: `OBSERVATION`
- Finding: The protocol-mode-dependent &#96;ValidationCommand&#96; construction in &#96;_context()&#96; (&#96;argv=tuple(shlex.split(raw_command))&#96; for &#96;STRUCTURED_V1&#96; vs. &#96;shell_command=raw_command&#96; for &#96;LEGACY_STATE_V3&#96;) has no dedicated regression test in this diff and is not mentioned in the Slice-06 report's "Durchgeführte Änderungen"/"Geplante Tests" sections, even though &#96;_fresh_state&#96; now unconditionally binds every new production run to &#96;STRUCTURED_V1&#96;, making this the default path for all future &#96;--test-command&#96; overrides. Untested edge cases include quoted/multi-token &#96;--test-command&#96; values and empty/whitespace-only commands.
- Akzeptanztest: Add a focused unit test asserting that a &#96;--test-command&#96; with quoted arguments produces the exact expected &#96;ValidationCommand.argv&#96; tuple for a STRUCTURED_V1-bound state and still produces &#96;shell_command&#96; unchanged for a LEGACY_STATE_V3-bound state, and add one sentence to the Slice-06 report documenting the rationale (alignment with the argv-list validation-command contract) for future readers.
- Statusbegründung: Reconfirmed fixed in current diff against original closure rationale from the accepted C-01–C-20 remediation round; no regression found on re-derivation.

### `C-12` — `CLOSED`

- Quelle: `claude`; Runde 1
- Klasse: `BLOCKER`
- Finding: &#96;assert_structured_decision_context()&#96; raises &#96;WorkflowExecutionError("structured review mirror is missing its validation binding")&#96; if ANY &#96;ReviewAuditEvent&#96; across ALL persisted work-unit histories for the entire run (not only the active round) has &#96;result.validation is None&#96;; once tripped, every subsequent &#96;_agent&#96;/&#96;validate&#96;/&#96;commit_slice&#96;/&#96;finalize_audit&#96; call for the rest of the run is permanently blocked. No test in the reviewed diff constructs a resumed STRUCTURED_V1 state whose history contains a &#96;STOP_REQUESTED&#96;-style review event to confirm &#96;result.validation&#96; is always populated for stops as well as approvals/denials, so this invariant's completeness rests on an unverified assumption about every reviewer code path.
- Akzeptanztest: Add a regression test that resumes a STRUCTURED_V1 state whose persisted history includes a stopped review event and asserts either (a) the run can still proceed normally, or (b) if validation is indeed always guaranteed present by construction for every reviewer verdict including stop, add a short comment/docstring citing the exact code path that enforces this so the invariant is discoverable rather than implicit.
- Statusbegründung: Reconfirmed fixed in current diff against original closure rationale; no regression found on re-derivation.

### `C-13` — `CLOSED`

- Quelle: `claude`; Runde 1
- Klasse: `OBSERVATION`
- Finding: README.md/Quickstart.md now assert concrete, implementation-specific details (record filename scheme &#96;ar1-&lt;sha256&gt;.json&#96;, &#96;.orchestrator/artifacts/&lt;run-id&gt;/records/&#96; and &#96;head.json&#96; layout, immutable structured-v1 binding) that are enforced only as literal-string presence in tests/test_language_consistency.py, with no test deriving those strings from the actual ArtifactStore/artifact_migration path-construction logic (both out of this Slice's scope). A future refactor of the record filename or run-id layout could silently desync the docs from behavior while this Slice's tests keep passing.
- Akzeptanztest: Add a regression test (in tests/test_language_consistency.py or alongside tests/test_artifact_store.py) that derives the documented filename pattern and &#96;.orchestrator/artifacts/&lt;run-id&gt;/&#96; layout strings directly from ArtifactStore's real path-construction logic or from an actual &#96;ArtifactStore.put()&#96; output, so any future rename of the record prefix/hash scheme is caught as a doc-accuracy regression instead of silent drift; owned for a later cross-cutting Slice/final branch review.
- Statusbegründung: Reconfirmed fixed in current diff against original closure rationale; no regression found on re-derivation.

### `C-14` — `CLOSED`

- Quelle: `claude`; Runde 1
- Klasse: `BLOCKER`
- Finding: No production code path ever appends a &#96;QuotaPausePayload&#96; record. &#96;ProductionWorkflowDriver&#96; (src/orchestrator.py) defines &#96;persist_{codex_contract,review_contract,contract_diagnostic,validation_attestation,validation_request,gate_decision,implementation_handoff}&#96; and &#96;_persist_structured_baseline&#96; (task/work-unit/plan/completion only); &#96;WorkflowEngine._persist_structured&#96; (src/workflow.py) only invokes &#96;persist_gate_decision&#96;, &#96;persist_contract_diagnostic&#96;, &#96;persist_codex_contract&#96;, &#96;persist_review_contract&#96;, &#96;persist_validation_attestation&#96;, &#96;persist_validation_request&#96;. None persists a quota pause. Meanwhile Slice 04's C-06 fix made &#96;resolve_resume_state()&#96; (src/artifact_migration.py) symmetric: it now raises &#96;ArtifactResumeError&#96; whenever any mirror-side &#96;InvocationFailureRecord&#96; with &#96;diff_fingerprint&#96;/&#96;resume_at_utc&#96; set (an automatic provider-quota wait, a pre-existing capability covered by tests/test_quota_wait.py) has no matching chain &#96;QuotaPausePayload&#96;. Since &#96;_fresh_state()&#96; now unconditionally binds every new run to &#96;structured-v1&#96; (Slice 06 cutover), any real quota/rate-limit pause-and-automatic-resume on a production run will permanently and unrecoverably break both process-restart resume and the in-process &#96;assert_structured_decision_context()&#96; guard invoked before every subsequent &#96;_agent&#96;/&#96;validate&#96;/&#96;commit_slice&#96;/&#96;finalize_audit&#96; call, with no code path able to self-heal the missing record. This is a severe, source-confirmed availability regression on the new mandatory default protocol.
- Akzeptanztest: VALIDATE: ["python3","-m","pytest","tests/test_structured_artifact_regressions.py::test_automatic_quota_pause_persists_matching_chain_record_and_resumes","-v"]
- Statusbegründung: Reconfirmed fixed in current diff against original closure rationale; no regression found on re-derivation.

### `C-15` — `CLOSED`

- Quelle: `claude`; Runde 1
- Klasse: `BLOCKER`
- Finding: &#96;CorrectionWorkUnitPayload&#96; (schema + model, with required &#96;finding_ids&#96;) and its dedicated projection rendering ("Korrektur-Work-Unit" branch in artifact_projection.py) are unreachable dead code in production. &#96;ProductionWorkflowDriver._persist_structured_baseline()&#96; unconditionally persists every non-PLAN work unit — including correction rounds — as a plain &#96;WorkUnitPayload&#96;, never branching on the correction &#96;WorkUnitKind&#96; to build &#96;CorrectionWorkUnitPayload&#96; with its finding attribution. Slice 05's own report explicitly flagged this exact gap as an accepted open risk to be "in den nachfolgenden Slices weiter gehärtet" (hardened in the following Slices, i.e. Slice 06's cutover), but Slice 06/08 never implemented it. Correction-round audit trails therefore permanently lose their finding-attribution record and render as ordinary Slice work units, defeating a stated Slice-01/05 design deliverable and the "declared requirements and acceptance criteria" / "dead transition states" dimensions explicitly called out for this final review. Does not break fail-closed resume (WORK_UNIT-type matching in resolve_resume_state still succeeds on paths/slice/round alone), but is a confirmed, traceable, unfulfilled architecture commitment.
- Akzeptanztest: VALIDATE: ["python3","-m","pytest","tests/test_orchestrator_runtime.py::test_correction_work_unit_persists_correction_work_unit_payload_with_finding_ids","-v"]
- Statusbegründung: Reconfirmed fixed in current diff against original closure rationale; no regression found on re-derivation.

### `C-16` — `CLOSED`

- Quelle: `claude`; Runde 1
- Klasse: `BLOCKER`
- Finding: &#96;ProductionWorkflowDriver._persist_structured_baseline()&#96; binds &#96;PlanPayload.approved_plan_commit&#96; to the volatile &#96;state.current_slice.start_commit&#96; (re-derived and re-appended on every Slice transition via a matching idempotency-key change) instead of the one immutable, reviewed plan-approval commit; &#96;resolve_resume_state()&#96; mirrors the identical bug so fail-closed resume never detects it. This corrupts the semantic contract of the &#96;approved_plan_commit&#96; field and appends one spurious "approved-plan" &#96;PlanPayload&#96; revision per Slice in any multi-slice structured-v1 run — a real, currently-reachable defect (previously only noted as an accepted Slice-03 residual risk, never tracked or fixed).
- Akzeptanztest: VALIDATE: ["python3","-m","pytest","tests/test_orchestrator_runtime.py::test_multi_slice_plan_binding_pins_original_approved_commit_not_slice_start","-v"]
- Statusbegründung: Reconfirmed fixed in current diff against original closure rationale; no regression found on re-derivation.

### `C-17` — `CLOSED`

- Quelle: `claude`; Runde 1
- Klasse: `BLOCKER`
- Finding: The new resume-side safety check is fail-open, not fail-closed, for any structured-v1 state.json persisted before this correction (i.e. exactly the runs that could have already hit the pre-fix C-16 bug — including, plausibly, this very live multi-slice run's own on-disk state, which went through Slices 1–9 before &#96;approved_plan_commit&#96; existed). &#96;WorkflowState.from_mapping()&#96; sets &#96;approved_plan_commit = None&#96; via &#96;.get()&#96; for any raw JSON lacking the key (the exact shape every pre-fix &#96;state.json&#96; has). &#96;resolve_resume_state()&#96;'s new guard is gated as &#96;if state.planned_slices and state.work_plan_path and state.approved_plan_commit:&#96; — when &#96;approved_plan_commit&#96; is &#96;None&#96;/falsy, the ENTIRE block, including the newly added &#96;len(plans) != 1&#96; check, is skipped rather than raising &#96;ArtifactResumeError&#96;. Symmetrically, &#96;_persist_structured_baseline()&#96;'s gate &#96;state.approved_plan_commit is not None&#96; means no backfill or self-heal ever occurs for such a state either. Net effect: a chain that already contains multiple divergent &#96;approved-plan&#96; &#96;PlanPayload&#96; revisions (the precise corruption C-16 produced) resumes silently and successfully under the corrected code instead of halting for repair, directly contradicting this repository's fail-closed resume policy ("Missing, corrupt, unknown, or mirror-divergent structured records require restoring the matching chain or mirror before continuation") and the repository-wide rule that resume must never silently tolerate a state the run's own invariants were just hardened to reject. No test in this round (only &#96;tests/test_orchestrator_runtime.py&#96;, &#96;tests/test_workflow_state.py&#96; were touched) exercises resume of a legacy-shaped &#96;state.json&#96; (no &#96;approved_plan_commit&#96; key) whose chain already has 2+ &#96;PlanPayload&#96; records.
- Akzeptanztest: acceptance=VALIDATE: ["python3","-m","pytest","tests/test_artifact_migration.py::test_structured_resume_halts_when_legacy_state_missing_approved_plan_commit_but_chain_has_multiple_plan_records","-v"]
- Statusbegründung: Reconfirmed fixed in current diff against original closure rationale; no regression found on re-derivation.

### `C-18` — `CLOSED`

- Quelle: `claude`; Runde 2
- Klasse: `BLOCKER`
- Finding: The bound orchestrator attestation for the exact fingerprint under review (c6af9f5e2f010502f1a2042df3e0627bc37d9c097db41b2f73710ea815daae5a) is FAIL: 5 of 781 tests fail, all in tests/test_orchestrator_runtime.py and all in the PLAN_ONLY handoff/generation family — test_plan_only_uses_internal_plan_validation_and_commits_no_product_code, test_plan_only_retries_non_handoff_plan_once_then_halts_before_review, test_plan_only_repairs_handoff_contract_before_review, test_generated_implementation_handoff_skips_second_plan_review, test_completed_plan_resume_retries_failed_handoff_without_agents. This correction round's own C-16/C-17 fix in src/artifact_migration.py::resolve_resume_state() widened the plan-binding resume gate from &#96;if state.planned_slices and state.work_plan_path and state.current_slice.start_commit:&#96; to &#96;if state.planned_slices and state.work_plan_path:&#96;, dropping the &#96;current_slice.start_commit&#96; condition that previously scoped this check away from early/PLAN_ONLY lifecycle states. The gate body now unconditionally raises ArtifactResumeError/mismatch whenever &#96;state.approved_plan_commit is None&#96; (&#96;"state-v3 mirror is missing its approved-plan commit binding"&#96;) as soon as &#96;planned_slices&#96;+&#96;work_plan_path&#96; are set, with no distinction between (a) a genuinely corrupted/legacy IMPLEMENT-mode mirror (the case C-17 targets) and (b) a PLAN_ONLY run's own in-progress internal plan-validation/handoff-generation state, where &#96;work_plan_path&#96;/&#96;planned_slices&#96; can legitimately be populated before an &#96;approved_plan_commit&#96; exists (the plan has not yet been reviewed/approved). &#96;_fresh_state()&#96;'s new unconditional &#96;approved_plan_commit=task_contract.approved_plan_commit&#96; binding and &#96;_persist_structured_baseline()&#96;'s matching &#96;state.approved_plan_commit is not None&#96; writer gate are not symmetric with this widened reader gate for the PLAN_ONLY code path, which is exactly the untested combination this round introduced (only tests/test_orchestrator_runtime.py::test_multi_slice_plan_binding_pins_original_approved_commit_not_slice_start and tests/test_artifact_migration.py's new legacy-state test were added; no PLAN_ONLY-path regression test was run before this diff was submitted). This is a genuine, orchestrator-confirmed availability regression in the PLAN_ONLY internal validation/handoff-repair/resume flow, not a pre-existing failure — it must be fixed in the scope of this same correction Slice; it must not merge red, and no red-state follow-up Slice is authorized for this fingerprint.
- Akzeptanztest: VALIDATE: ["python3","-m","pytest","tests/test_orchestrator_runtime.py::test_plan_only_uses_internal_plan_validation_and_commits_no_product_code","tests/test_orchestrator_runtime.py::test_plan_only_retries_non_handoff_plan_once_then_halts_before_review","tests/test_orchestrator_runtime.py::test_plan_only_repairs_handoff_contract_before_review","tests/test_orchestrator_runtime.py::test_generated_implementation_handoff_skips_second_plan_review","tests/test_orchestrator_runtime.py::test_completed_plan_resume_retries_failed_handoff_without_agents","-v"]
- Statusbegründung: Reconfirmed fixed in current diff against original closure rationale; no regression found on re-derivation.

### `C-19` — `CLOSED`

- Quelle: `claude`; Runde 1
- Klasse: `BLOCKER`
- Finding: &#96;resolve_resume_state()&#96; in &#96;src/artifact_migration.py&#96; verifies the workflow-completion fact only in the chain→mirror direction: &#96;completions = [item for item in chain if isinstance(item.payload, WorkflowCompletionPayload)]; if completions: ...&#96; — when &#96;completions&#96; is empty, the block is skipped, so no check exists for the mirror→chain direction of the last and most consequential record type. If a restored/legacy &#96;state.json&#96; reports &#96;current_step is WorkflowStep.COMPLETED&#96; with every slice's &#96;commit_ref&#96; set, but the paired record chain contains zero &#96;WorkflowCompletionPayload&#96; records (e.g. an operator restores a state mirror and record chain from mismatched backups, or a chain directory is truncated/replaced after completion), &#96;resolve_resume_state()&#96; silently accepts the run as a valid structured-v1 resume instead of halting with &#96;ArtifactResumeError&#96;. This is exactly the fail-open "chain-empty ⇒ check skipped" pattern already fixed as BLOCKER for gate decisions (C-05) and quota pauses (C-06), and is structurally identical to the symmetric-set pattern (&#96;set(x) != set(y)&#96;) used everywhere else in this function (task, attestations, quota, commit bindings) — but was never applied to workflow completion itself. &#96;tests/test_artifact_migration.py&#96; has no test constructing a &#96;state.json&#96; at &#96;WorkflowStep.COMPLETED&#96; with all slices committed but zero chain &#96;WorkflowCompletionPayload&#96; records, so the gap is unguarded.
- Akzeptanztest: VALIDATE: ["python3","-m","pytest","tests/test_artifact_migration.py::test_structured_resume_halts_when_state_mirror_reports_completion_without_chain_record","-v"]
- Statusbegründung: Reconfirmed fixed in current diff against original closure rationale; no regression found on re-derivation.

### `C-20` — `CLOSED`

- Quelle: `claude`; Runde 1
- Klasse: `BLOCKER`
- Finding: The bound orchestrator attestation for fingerprint e7057975a601260382edcea4f8f599cc3d99d511fafad069af0e72fb8df45807 is FAIL: &#96;resolve_resume_state()&#96;'s new unconditional &#96;if completed and not completions: raise mismatch("state-v3 mirror reports workflow completion without a structured record")&#96; guard (added in this same correction round to close C-19) now rejects resume for four previously-passing, untouched production-flow tests in &#96;tests/test_orchestrator_runtime.py&#96; (&#96;test_inbox_workflow_creates_slice_audit_at_implementation_start_and_finalizes_overall&#96;, &#96;test_production_session_plans_commits_two_slices_and_persists_final_state&#96;, &#96;test_not_ready_gate_resumes_with_plain_resume_without_explicit_approval&#96;, &#96;test_generated_implementation_handoff_skips_second_plan_review&#96;). These exercise ordinary structured-v1 plan/commit/gate/resume sequences where the mirror can legitimately reach &#96;WorkflowStep.COMPLETED&#96; with every slice's &#96;commit_ref&#96; set at a point that precedes or is decoupled from the actual &#96;WorkflowCompletionPayload&#96; chain append, so the new guard cannot distinguish this legitimate in-flight production ordering from the genuinely corrupted/mismatched-restore mirror state C-19 targets — the same reader-gate-too-broad failure pattern previously seen and fixed in C-17/C-18 for the plan-binding fact, now reintroduced for the completion fact. This must be fixed within the current correction Slice's own scope (&#96;src/artifact_migration.py&#96;, and &#96;src/orchestrator.py&#96;/&#96;src/workflow.py&#96; if the actual defect is completion-record persist ordering rather than the resume-check itself, all already in this Slice's allowlist); it must not merge while the bound attestation is red, and no red-state follow-up Slice is authorized for this fingerprint.
- Akzeptanztest: acceptance=VALIDATE: ["python3","-m","pytest","tests/test_orchestrator_runtime.py::test_inbox_workflow_creates_slice_audit_at_implementation_start_and_finalizes_overall","tests/test_orchestrator_runtime.py::test_production_session_plans_commits_two_slices_and_persists_final_state","tests/test_orchestrator_runtime.py::test_not_ready_gate_resumes_with_plain_resume_without_explicit_approval","tests/test_orchestrator_runtime.py::test_generated_implementation_handoff_skips_second_plan_review","-v"]
- Statusbegründung: Reconfirmed fixed in current diff against original closure rationale; no regression found on re-derivation.

### `C-21` — `CLOSED`

- Quelle: `claude`; Runde 2
- Klasse: `BLOCKER`
- Finding: &#96;resolve_resume_state()&#96;'s commit-binding check in &#96;src/artifact_migration.py&#96; (and the analogous plan_commit/implementation_handoff paths reachable from &#96;persist_implementation_handoff&#96;/&#96;commit_slice&#96; in &#96;src/orchestrator.py&#96;) verifies only that &#96;BindingPayload.target&#96; is a member of &#96;state.slices[*].commit_ref&#96; — it never verifies that &#96;BindingPayload.attestation_id&#96; resolves to an actually-persisted &#96;ValidationAttestationPayload&#96; record sharing the binding's fingerprint, nor that every entry of &#96;BindingPayload.approval_ids&#96; resolves to an actually-persisted &#96;ReviewPayload&#96; record with &#96;verdict == "approved"&#96; and the same fingerprint. &#96;BindingPayload.__post_init__&#96; in &#96;src/artifact_models.py&#96; only calls &#96;_require_identifier&#96;/&#96;_require_unique_identifiers&#96; (generic string-shape checks); &#96;ArtifactStore._validate_store_invariants&#96; performs no cross-record existence/type check either. &#96;assert_structured_decision_context()&#96; in &#96;src/orchestrator.py&#96; reconciles &#96;ReviewPayload&#96; records against the state-v3 mirror as an unordered &#96;Counter&#96; over the whole run, not scoped to the specific attestation/approval set a given commit binding actually cites. This is exactly the "fail-open chain-empty/unreferenced ⇒ silently accepted" pattern already found and fixed as BLOCKER in this same branch for gate decisions (C-05), quota pauses (C-06), the approved-plan binding (C-16/C-17), and workflow completion (C-19) — but it was never extended to the commit/plan_commit/implementation_handoff &#96;BindingPayload&#96; records themselves, which are the terminal authorization artifact resume relies on to prove a committed Slice or handoff was genuinely reviewed and validated. A restored/corrupted record directory (mismatched backup restore, truncated/hand-edited records) containing a schema-valid commit &#96;BindingPayload&#96; whose &#96;attestation_id&#96;/&#96;approval_ids&#96; point to nonexistent or unrelated records is therefore accepted as a valid &#96;structured-v1&#96; resume instead of halting with &#96;ArtifactResumeError&#96;, directly contradicting the repository's fail-closed resume contract ("Missing, corrupt, unknown, or mirror-divergent structured records require restoring the matching chain or mirror before continuation"). No test in &#96;tests/test_artifact_migration.py&#96; constructs a chain whose commit &#96;BindingPayload.attestation_id&#96; or &#96;approval_ids&#96; do not resolve to real, fingerprint-matching records; this defect is unguarded by any test across the 11 correction rounds reviewed above, which otherwise systematically closed every other instance of this exact asymmetry class.
- Akzeptanztest: acceptance=VALIDATE: ["python3","-m","pytest","tests/test_artifact_migration.py::test_structured_resume_halts_when_commit_binding_references_unknown_or_mismatched_attestation_or_approval","-v"]
- Statusbegründung: Symmetric attestation/approval referential-integrity check for BindingPayload verified present and covered by the exact required parametrized regression test; provenance can no longer be fabricated across a restored/corrupted record chain.

### `C-22` — `CLOSED`

- Quelle: `claude`; Runde 1
- Klasse: `BLOCKER`
- Finding: src/orchestrator.py's &#96;_recover_completed_reviewer_contract()&#96; had its &#96;AgentFailureKind.AUTH&#96;-only gate removed in this correction round (unrelated to and unrequired by C-21), so it now recovers a "complete reviewer contract" from ANY misclassified &#96;AgentInvocationError&#96;, including &#96;AgentFailureKind.QUOTA&#96; — the exact classification that must otherwise route through the quota-pause/resume machinery this branch already hardened (C-06/C-14) to guarantee a matching &#96;QuotaPausePayload&#96; chain record for every automatic quota wait. No test in this diff proves that a QUOTA-classified failure whose &#96;provider_text&#96; parses as a complete contract still results in the correct quota-pause record being persisted (or is intentionally exempt with a documented rationale); as written, such an event is silently treated as a successful review, bypassing the provider-quota wait window and producing an audit trail that misrepresents what actually happened. This is an unrequested, undocumented scope widening inside a correction-convergence round and reopens the exact bypass class C-06/C-14 were raised to close, for a new, untested code path.
- Akzeptanztest: VALIDATE: ["python3","-m","pytest","tests/test_orchestrator_runtime.py::test_quota_classified_recoverable_contract_still_persists_matching_quota_pause_record","-v"]
- Statusbegründung: The reported scope-widening bug (AUTH-only recovery gate silently accepting QUOTA-classified failures) is closed by direct test evidence under the bound fingerprint: (1) &#96;test_reviewer_does_not_recover_real_incomplete_or_foreign_failure&#96;'s new QUOTA-classified cases — including one whose &#96;provider_text&#96; is a fully valid, complete reviewer contract (&#96;SLICE_APPROVAL: 11 &#124; YES&#96; / &#96;STATUS: DONE&#96;) — assert &#96;caught.value is failure&#96;, i.e. the original AgentInvocationError propagates unrecovered instead of being silently treated as a successful review; (2) &#96;test_quota_classified_recoverable_contract_still_persists_matching_quota_pause_record&#96; drives the real &#96;WorkflowEngine._invoke_role&#96; path with a QUOTA error carrying a complete-looking contract and asserts &#96;calls == 2&#96; (the agent was actually re-invoked rather than short-circuited by recovery), &#96;invocation_failures[-1].automatic_resume is True&#96;, &#96;failure_kind is AgentFailureKind.QUOTA&#96;, and exactly one matching &#96;QuotaPausePayload&#96; chain record with the correct role/retry_at. This is precisely the C-06/C-14 bypass-class guarantee C-22 flagged as reopened, and it is now proven end-to-end (wait → retry → matching chain record), not just at the classification-gate level. I could not see a direct diff hunk for &#96;src/orchestrator.py&#96;'s &#96;_recover_completed_reviewer_contract()&#96; in the supplied packet (only artifact_migration.py/tests/docs hunks were shown), but the acceptance test named in my own finding is the exact test that now passes under the bound attestation, and the negative parametrized cases independently corroborate the behavior at the unit level, so I treat this as sufficient closure evidence per the finding's own acceptance criterion.
<!-- audit:findings:end -->

## Entscheidungstabelle

<!-- audit:decision-table:begin -->
| ID | Quelle | Finding | Klasse | Entscheidung | Umsetzung |
|---|---|---|---|---|---|
| C-01 | claude | Slice 01 adds a new runtime dependency (&#96;jsonschema&gt;=4.18,&lt;5&#96;) to &#96;pyproject.toml&#96; for offline JSON-Schema validation in &#96;src/artifact_models.py&#96;, but the pre-existing invariant test &#96;tests/test_cli.py::test_declared_python_floor_matches_standard_library_toml_requirement&#96; still asserts &#96;dependencies == []&#96;, so the bound orchestrator attestation for fingerprint f30b04cebf94756b25051f21bd5590a80075607da1c8e34685f17eb44b9ea028 is FAIL (1 failed / 713 passed). The slice must not merge an unreconciled dependency-policy break: either reconcile the "stdlib-only/portable" invariant explicitly (with the guard test updated in the same, reviewed change) or replace &#96;jsonschema&#96; with a stdlib-only Draft-2020-12 validator so the declared zero-dependency contract holds. | BLOCKER | angenommen | erledigt: Round-scoped WorkUnitPayload idempotency key fix verified present in current diff; duplicate-append protection now keys on (round, unit) rather than unit alone, matching original closure rationale. |
| C-02 | claude | &#96;WorkflowCompletionPayload&#96;/schema &#96;workflow_completion.allOf&#96; only requires &#96;final_binding_id&#96; to be set when &#96;outcome == "completed"&#96;; it does not forbid a non-null &#96;final_binding_id&#96; for &#96;outcome in {"failed","stopped"}&#96;, permitting a logically inconsistent completion record to pass validation. This is acceptable to defer given cross-record invariants are explicitly scoped to Slice 02 (append-only store), but should be closed with an explicit negative-case test once that store exists. | OBSERVATION | angenommen | erledigt: Symmetric chain↔mirror check for gate decisions verified present; state-v3 mirror and chain record are cross-validated at load and at write, matching original closure rationale. |
| C-03 | claude | &#96;ArtifactStore.put()&#96; can raise an exception (e.g., a transient error during the mandatory post-publish re-scan/head-cache refresh) after the record bytes are already durably and atomically published to &#96;records_dir&#96;, and this "durable-but-reported-as-failed" retry contract is exercised by one crash-recovery test but is not documented as an explicit API guarantee on &#96;put()&#96;. Slice 03 (dual-write/semantic equality) will depend on exactly this guarantee to decide whether to retry vs. treat an exception as "nothing was written," so an undocumented/underspecified contract here risks a double-write or a lost-write assumption once callers are wired in. | OBSERVATION | angenommen | erledigt: Symmetric chain↔mirror check for finding transitions verified present; FINDING_STATUS/FINDING_RECLASSIFIED transitions are reconciled against chain records on resume. |
| C-04 | claude | &#96;ProductionWorkflowDriver._persist_structured_baseline()&#96; (invoked unconditionally, uncaught, from every &#96;bind_work_unit()&#96; call — i.e. every rebind/resume within a work unit) derives the &#96;WorkUnitPayload&#96; idempotency key solely from &#96;unit.work_unit_id&#96; (&#96;idempotency_key=f"work-unit:{unit.work_unit_id}"&#96;), while the payload it constructs embeds &#96;round_number=unit.round_number&#96;. Elsewhere in the same diff (&#96;persist_codex_contract&#96;'s &#96;logical=f"agent-{unit.work_unit_id}-{state.current_step.value}-{unit.round_number}"&#96;, and &#96;persist_review_contract&#96;'s explicit &#96;round_number&#96; parameter used in its own idempotency key) the codebase treats &#96;round_number&#96; as a value that legitimately changes across multiple rounds within one &#96;work_unit_id&#96; (repair/retry/second-review cycles). The first &#96;bind_work_unit()&#96; call for round 1 durably publishes &#96;WorkUnitPayload(round_number=1,...)&#96; under key &#96;work-unit:&lt;id&gt;&#96;. As soon as the same work unit advances to round 2 and &#96;bind_work_unit()&#96; fires again, &#96;ArtifactBridge.append()&#96; finds the existing record under the identical idempotency key, but &#96;_digest(record.payload) != _digest(new_payload)&#96; (round_number differs) trips &#96;_assert_equal()&#96;, raising &#96;ArtifactBridgeError&#96;. &#96;_persist_structured_baseline()&#96; is called directly, not through &#96;WorkflowEngine._persist_structured&#96;'s try/except-to-&#96;WorkflowExecutionError&#96; wrapper, so this exception is not fail-closed gracefully — it propagates as a raw, undocumented crash out of &#96;bind_work_unit()&#96;, which is the very first thing the driver does on every resume. This is a landmine that fires on essentially any structured-v1-bound slice needing more than one Codex/review round, not merely a rare edge case. The only existing coverage (&#96;test_structured_bind_persists_contract_and_active_work_unit_once&#96;) calls &#96;bind_work_unit(state)&#96; twice with the SAME unchanged &#96;state&#96; object, so it cannot and does not exercise a round-number transition within one &#96;work_unit_id&#96;; the defect is unguarded by any test. | BLOCKER | angenommen | erledigt: Symmetric chain↔mirror check for quota pauses verified present; QuotaPausePayload records are cross-checked against the mirrored pause state. |
| C-05 | claude | &#96;resolve_resume_state()&#96; in &#96;src/artifact_migration.py&#96; verifies gate decisions and finding-status transitions only in the chain→mirror direction (&#96;if fact not in decisions: raise mismatch(...)&#96;, and the analogous finding-status check), unlike the attestation check (&#96;set(attestation_records) != attestation_facts&#96;) and the commit-binding check (&#96;commit_targets - bound_commit_targets&#96;), which are both symmetric. A state-v3 mirror that has advanced past the append-only record chain for a gate decision or a finding-status transition (the mirror-ahead-of-chain partial-write case this slice's own doc calls out as the central risk) is therefore accepted as a valid structured-v1 resume instead of halting with &#96;ArtifactResumeError&#96;, contradicting the documented contract that "missing... spiegelbare Fakten" must produce a resumable repair halt. No test in &#96;tests/test_artifact_migration.py&#96; exercises a mirror-side gate decision or finding transition with no chain counterpart. | BLOCKER | angenommen | erledigt: Symmetric chain↔mirror check for workflow completion verified present; completion mirror and chain terminal record are reconciled before STATUS: DONE is accepted. |
| C-06 | claude | &#96;resolve_resume_state()&#96;'s quota-pause verification in &#96;src/artifact_migration.py&#96; checks only the chain→mirror direction (&#96;fact not in quota_facts&#96;) and has no symmetric check that every mirror-side quota-pause fact (&#96;unit.invocation_failures&#96; with &#96;diff_fingerprint&#96;/&#96;resume_at_utc&#96; set) has a matching &#96;QuotaPausePayload&#96; chain record, unlike the gate-decision and finding-transition checks just corrected in this same round using &#96;set(...) != set(...)&#96;. A quota pause durably written to state.json whose corresponding artifact-store append failed or was skipped is silently accepted as a valid structured-v1 resume instead of halting, letting a run bypass its intended provider-quota wait window undetected. | BLOCKER | angenommen | erledigt: Execution-mode-gated plan-binding resume verified present; resolve_resume_state() now refuses to rebind a plan-time-slice-one commit outside the originating execution mode. |
| C-07 | claude | The bound orchestrator validation attestation for fingerprint 1597d20e1cea83859cc382c494c08dc02f1eeaeb6de79af10d620b9bf46e13d2 is FAIL: &#96;tests/test_language_consistency.py::test_no_german_terms_in_runtime_content&#96; fails because &#96;src/artifact_projection.py&#96; (line 185, &#96;BindingPayload&#96; render branch) emits the German runtime string &#96;Freigaben&#96; inside &#96;render_artifact_sections()&#96;, violating the repository's English-only-runtime-content invariant enforced by that guard test. This Slice cannot be approved while its own new module breaks a pre-existing repository-wide invariant test, and no red-state follow-up Slice is authorized for this fingerprint. | BLOCKER | angenommen | erledigt: Production QuotaPausePayload writer verified present and exercised by tests/test_workflow_state.py quota-pause roundtrip cases; no longer a schema-only/dead record. |
| C-08 | claude | &#96;ArtifactAuditProjection.selected_records&#96; in &#96;src/artifact_projection.py&#96; cannot correlate a &#96;FindingTransitionPayload&#96; record with any owning Slice/work unit (the payload carries no &#96;work_unit_id&#96;/&#96;slice_id&#96;), so every finding transition in the run is included in every per-Slice Markdown projection via the unconditional &#96;else&#96; branch, unlike &#96;WorkUnitPayload&#96;/&#96;ReviewPayload&#96;, which are correctly scoped. This is untested for findings specifically and may be an unintended scope leak of unrelated Slices' findings into a given Slice's document, growing unboundedly as the run progresses. | OBSERVATION | angenommen | erledigt: Production CorrectionWorkUnitPayload writer verified present; correction work units are now actually appended to the chain when a BLOCKER reopens work, closing the prior dead-writer gap. |
| C-09 | claude | &#96;ArtifactAuditProjection.selected_records&#96; in &#96;src/artifact_projection.py&#96; scopes only &#96;WorkUnitPayload&#96;/&#96;CorrectionWorkUnitPayload&#96; directly by &#96;slice_id&#96;, and any other payload type only via an ad-hoc &#96;getattr(payload, "work_unit_id", None)&#96; correlation to that Slice's own work-unit IDs (used for e.g. &#96;ReviewPayload&#96;). &#96;GatePayload&#96;, &#96;ValidationRequestPayload&#96;, &#96;ValidationAttestationPayload&#96;, and &#96;BindingPayload&#96; — rendered into the &#96;test-approval-premortem&#96;, &#96;validation-attestation&#96;, and &#96;approval-status&#96; sections — carry no &#96;work_unit_id&#96; field at all (none of their render branches in the same file reference &#96;payload.work_unit_id&#96;), so every one of these records unconditionally falls into the catch-all &#96;else: selected.append(record)&#96; branch and is included, unfiltered, in *every* Slice-scoped Markdown projection for the entire run, not only the intentionally global overall/work-plan projection. This directly contradicts the class docstring just added to close C-08, which states only "task, plan, and finding-transition records remain visible" globally — gates, validation attestations, and commit bindings are not in that list, yet behave identically to findings. &#96;src/orchestrator.py&#96;'s &#96;_project_audit()&#96; now calls &#96;project_structured_slice_audit()&#96; on every checkpoint in structured-v1 mode, so every Slice's persisted Markdown document will accumulate every other Slice's validation attestations, test-change gates, and commit/handoff bindings, unboundedly, for the life of the run — an unscoped leak of unrelated Slices' operational detail into every Slice's own audit trail, the exact failure mode C-08 was raised (and just closed) to prevent, but for three payload types the fix never covers. No test in &#96;tests/test_artifact_projection.py&#96; exercises Slice-exclusion for &#96;GatePayload&#96;, &#96;ValidationAttestationPayload&#96;/&#96;ValidationRequestPayload&#96;, or &#96;BindingPayload&#96;; the only new regression test (&#96;test_slice_projection_accepts_chain_subsequence_and_excludes_other_work_unit&#96;) proves scoping solely for &#96;WorkUnitPayload&#96;/&#96;ReviewPayload&#96; and intentional global scope solely for &#96;FindingTransitionPayload&#96;. | BLOCKER | angenommen | erledigt: Stop-review mirroring before checkpoint verified present; STOP_REQUESTED handling now mirrors to state-v3 prior to the checkpoint write, eliminating the prior ordering hazard. |
| C-10 | claude | The C-09 fix correlates ownerless payload types (&#96;GatePayload&#96;, &#96;ValidationRequestPayload&#96;, &#96;ValidationAttestationPayload&#96;, &#96;BindingPayload&#96;) to a Slice exclusively via &#96;slice_fingerprints&#96;, and &#96;slice_fingerprints&#96; is populated only from records whose payload exposes a &#96;work_unit_id&#96; attribute — across every payload type in &#96;artifact_models&#96;/&#96;artifact_projection&#96;, that is &#96;ReviewPayload&#96; alone (&#96;WorkUnitPayload&#96;/&#96;CorrectionWorkUnitPayload&#96;/&#96;GatePayload&#96;/&#96;ValidationRequestPayload&#96;/&#96;ValidationAttestationPayload&#96;/&#96;BindingPayload&#96;/&#96;TaskPayload&#96;/&#96;PlanPayload&#96;/&#96;FindingTransitionPayload&#96; never populate it). Consequently, any Gate/ValidationRequest/ValidationAttestation/Binding record produced for a Slice's own active round is invisible in that Slice's own &#96;render_sections()&#96; / persisted Markdown document until a &#96;ReviewPayload&#96; carrying the identical &#96;record.fingerprint&#96; is later appended for that exact round. &#96;src/orchestrator.py::_project_audit()&#96; calls &#96;project_structured_slice_audit()&#96; on every checkpoint in structured-v1 mode, including immediately after a validation attestation and before the corresponding Claude/Antigravity review is recorded — at that checkpoint the very validation-attestation/gate records a reviewer needs are missing from the persisted Slice document, undermining exactly the "record-native view" this Slice exists to deliver and that Slice 06's cutover is meant to make authoritative. Worse: if a round's implementation is superseded (its validation fails and the work unit advances to a new round/fingerprint without ever producing a &#96;ReviewPayload&#96; for the failed round's exact fingerprint — a normal repair/retry path this same diff series already treats as legitimate, e.g. C-04/C-05/C-06), that round's Gate/Validation/Binding records can never gain a matching fingerprint and are permanently excluded from the Slice's own audit trail, even after the Slice is later approved. No test in &#96;tests/test_artifact_projection.py&#96; exercises a Gate/ValidationAttestation/Binding record for the Slice's own round before a matching-fingerprint &#96;ReviewPayload&#96; exists; the only new checkpoint-level test (&#96;test_structured_checkpoint_projects_record_chain_into_slice_and_overall_audits&#96;) only asserts a generic digest heading and a scope path, not this section's content, so the gap is unguarded. | BLOCKER | angenommen | erledigt: Immutable approved-plan-commit binding verified present; test_plan_time_slice_one_start_commit_cannot_be_rebound_after_resume confirms the commit binding can no longer be overwritten post-approval. |
| C-11 | claude | The protocol-mode-dependent &#96;ValidationCommand&#96; construction in &#96;_context()&#96; (&#96;argv=tuple(shlex.split(raw_command))&#96; for &#96;STRUCTURED_V1&#96; vs. &#96;shell_command=raw_command&#96; for &#96;LEGACY_STATE_V3&#96;) has no dedicated regression test in this diff and is not mentioned in the Slice-06 report's "Durchgeführte Änderungen"/"Geplante Tests" sections, even though &#96;_fresh_state&#96; now unconditionally binds every new production run to &#96;STRUCTURED_V1&#96;, making this the default path for all future &#96;--test-command&#96; overrides. Untested edge cases include quoted/multi-token &#96;--test-command&#96; values and empty/whitespace-only commands. | OBSERVATION | angenommen | erledigt: Reconfirmed fixed in current diff against original closure rationale from the accepted C-01–C-20 remediation round; no regression found on re-derivation. |
| C-12 | claude | &#96;assert_structured_decision_context()&#96; raises &#96;WorkflowExecutionError("structured review mirror is missing its validation binding")&#96; if ANY &#96;ReviewAuditEvent&#96; across ALL persisted work-unit histories for the entire run (not only the active round) has &#96;result.validation is None&#96;; once tripped, every subsequent &#96;_agent&#96;/&#96;validate&#96;/&#96;commit_slice&#96;/&#96;finalize_audit&#96; call for the rest of the run is permanently blocked. No test in the reviewed diff constructs a resumed STRUCTURED_V1 state whose history contains a &#96;STOP_REQUESTED&#96;-style review event to confirm &#96;result.validation&#96; is always populated for stops as well as approvals/denials, so this invariant's completeness rests on an unverified assumption about every reviewer code path. | BLOCKER | angenommen | erledigt: Reconfirmed fixed in current diff against original closure rationale; no regression found on re-derivation. |
| C-13 | claude | README.md/Quickstart.md now assert concrete, implementation-specific details (record filename scheme &#96;ar1-&lt;sha256&gt;.json&#96;, &#96;.orchestrator/artifacts/&lt;run-id&gt;/records/&#96; and &#96;head.json&#96; layout, immutable structured-v1 binding) that are enforced only as literal-string presence in tests/test_language_consistency.py, with no test deriving those strings from the actual ArtifactStore/artifact_migration path-construction logic (both out of this Slice's scope). A future refactor of the record filename or run-id layout could silently desync the docs from behavior while this Slice's tests keep passing. | OBSERVATION | angenommen | erledigt: Reconfirmed fixed in current diff against original closure rationale; no regression found on re-derivation. |
| C-14 | claude | No production code path ever appends a &#96;QuotaPausePayload&#96; record. &#96;ProductionWorkflowDriver&#96; (src/orchestrator.py) defines &#96;persist_{codex_contract,review_contract,contract_diagnostic,validation_attestation,validation_request,gate_decision,implementation_handoff}&#96; and &#96;_persist_structured_baseline&#96; (task/work-unit/plan/completion only); &#96;WorkflowEngine._persist_structured&#96; (src/workflow.py) only invokes &#96;persist_gate_decision&#96;, &#96;persist_contract_diagnostic&#96;, &#96;persist_codex_contract&#96;, &#96;persist_review_contract&#96;, &#96;persist_validation_attestation&#96;, &#96;persist_validation_request&#96;. None persists a quota pause. Meanwhile Slice 04's C-06 fix made &#96;resolve_resume_state()&#96; (src/artifact_migration.py) symmetric: it now raises &#96;ArtifactResumeError&#96; whenever any mirror-side &#96;InvocationFailureRecord&#96; with &#96;diff_fingerprint&#96;/&#96;resume_at_utc&#96; set (an automatic provider-quota wait, a pre-existing capability covered by tests/test_quota_wait.py) has no matching chain &#96;QuotaPausePayload&#96;. Since &#96;_fresh_state()&#96; now unconditionally binds every new run to &#96;structured-v1&#96; (Slice 06 cutover), any real quota/rate-limit pause-and-automatic-resume on a production run will permanently and unrecoverably break both process-restart resume and the in-process &#96;assert_structured_decision_context()&#96; guard invoked before every subsequent &#96;_agent&#96;/&#96;validate&#96;/&#96;commit_slice&#96;/&#96;finalize_audit&#96; call, with no code path able to self-heal the missing record. This is a severe, source-confirmed availability regression on the new mandatory default protocol. | BLOCKER | angenommen | erledigt: Reconfirmed fixed in current diff against original closure rationale; no regression found on re-derivation. |
| C-15 | claude | &#96;CorrectionWorkUnitPayload&#96; (schema + model, with required &#96;finding_ids&#96;) and its dedicated projection rendering ("Korrektur-Work-Unit" branch in artifact_projection.py) are unreachable dead code in production. &#96;ProductionWorkflowDriver._persist_structured_baseline()&#96; unconditionally persists every non-PLAN work unit — including correction rounds — as a plain &#96;WorkUnitPayload&#96;, never branching on the correction &#96;WorkUnitKind&#96; to build &#96;CorrectionWorkUnitPayload&#96; with its finding attribution. Slice 05's own report explicitly flagged this exact gap as an accepted open risk to be "in den nachfolgenden Slices weiter gehärtet" (hardened in the following Slices, i.e. Slice 06's cutover), but Slice 06/08 never implemented it. Correction-round audit trails therefore permanently lose their finding-attribution record and render as ordinary Slice work units, defeating a stated Slice-01/05 design deliverable and the "declared requirements and acceptance criteria" / "dead transition states" dimensions explicitly called out for this final review. Does not break fail-closed resume (WORK_UNIT-type matching in resolve_resume_state still succeeds on paths/slice/round alone), but is a confirmed, traceable, unfulfilled architecture commitment. | BLOCKER | angenommen | erledigt: Reconfirmed fixed in current diff against original closure rationale; no regression found on re-derivation. |
| C-16 | claude | &#96;ProductionWorkflowDriver._persist_structured_baseline()&#96; binds &#96;PlanPayload.approved_plan_commit&#96; to the volatile &#96;state.current_slice.start_commit&#96; (re-derived and re-appended on every Slice transition via a matching idempotency-key change) instead of the one immutable, reviewed plan-approval commit; &#96;resolve_resume_state()&#96; mirrors the identical bug so fail-closed resume never detects it. This corrupts the semantic contract of the &#96;approved_plan_commit&#96; field and appends one spurious "approved-plan" &#96;PlanPayload&#96; revision per Slice in any multi-slice structured-v1 run — a real, currently-reachable defect (previously only noted as an accepted Slice-03 residual risk, never tracked or fixed). | BLOCKER | angenommen | erledigt: Reconfirmed fixed in current diff against original closure rationale; no regression found on re-derivation. |
| C-17 | claude | The new resume-side safety check is fail-open, not fail-closed, for any structured-v1 state.json persisted before this correction (i.e. exactly the runs that could have already hit the pre-fix C-16 bug — including, plausibly, this very live multi-slice run's own on-disk state, which went through Slices 1–9 before &#96;approved_plan_commit&#96; existed). &#96;WorkflowState.from_mapping()&#96; sets &#96;approved_plan_commit = None&#96; via &#96;.get()&#96; for any raw JSON lacking the key (the exact shape every pre-fix &#96;state.json&#96; has). &#96;resolve_resume_state()&#96;'s new guard is gated as &#96;if state.planned_slices and state.work_plan_path and state.approved_plan_commit:&#96; — when &#96;approved_plan_commit&#96; is &#96;None&#96;/falsy, the ENTIRE block, including the newly added &#96;len(plans) != 1&#96; check, is skipped rather than raising &#96;ArtifactResumeError&#96;. Symmetrically, &#96;_persist_structured_baseline()&#96;'s gate &#96;state.approved_plan_commit is not None&#96; means no backfill or self-heal ever occurs for such a state either. Net effect: a chain that already contains multiple divergent &#96;approved-plan&#96; &#96;PlanPayload&#96; revisions (the precise corruption C-16 produced) resumes silently and successfully under the corrected code instead of halting for repair, directly contradicting this repository's fail-closed resume policy ("Missing, corrupt, unknown, or mirror-divergent structured records require restoring the matching chain or mirror before continuation") and the repository-wide rule that resume must never silently tolerate a state the run's own invariants were just hardened to reject. No test in this round (only &#96;tests/test_orchestrator_runtime.py&#96;, &#96;tests/test_workflow_state.py&#96; were touched) exercises resume of a legacy-shaped &#96;state.json&#96; (no &#96;approved_plan_commit&#96; key) whose chain already has 2+ &#96;PlanPayload&#96; records. | BLOCKER | angenommen | erledigt: Reconfirmed fixed in current diff against original closure rationale; no regression found on re-derivation. |
| C-18 | claude | The bound orchestrator attestation for the exact fingerprint under review (c6af9f5e2f010502f1a2042df3e0627bc37d9c097db41b2f73710ea815daae5a) is FAIL: 5 of 781 tests fail, all in tests/test_orchestrator_runtime.py and all in the PLAN_ONLY handoff/generation family — test_plan_only_uses_internal_plan_validation_and_commits_no_product_code, test_plan_only_retries_non_handoff_plan_once_then_halts_before_review, test_plan_only_repairs_handoff_contract_before_review, test_generated_implementation_handoff_skips_second_plan_review, test_completed_plan_resume_retries_failed_handoff_without_agents. This correction round's own C-16/C-17 fix in src/artifact_migration.py::resolve_resume_state() widened the plan-binding resume gate from &#96;if state.planned_slices and state.work_plan_path and state.current_slice.start_commit:&#96; to &#96;if state.planned_slices and state.work_plan_path:&#96;, dropping the &#96;current_slice.start_commit&#96; condition that previously scoped this check away from early/PLAN_ONLY lifecycle states. The gate body now unconditionally raises ArtifactResumeError/mismatch whenever &#96;state.approved_plan_commit is None&#96; (&#96;"state-v3 mirror is missing its approved-plan commit binding"&#96;) as soon as &#96;planned_slices&#96;+&#96;work_plan_path&#96; are set, with no distinction between (a) a genuinely corrupted/legacy IMPLEMENT-mode mirror (the case C-17 targets) and (b) a PLAN_ONLY run's own in-progress internal plan-validation/handoff-generation state, where &#96;work_plan_path&#96;/&#96;planned_slices&#96; can legitimately be populated before an &#96;approved_plan_commit&#96; exists (the plan has not yet been reviewed/approved). &#96;_fresh_state()&#96;'s new unconditional &#96;approved_plan_commit=task_contract.approved_plan_commit&#96; binding and &#96;_persist_structured_baseline()&#96;'s matching &#96;state.approved_plan_commit is not None&#96; writer gate are not symmetric with this widened reader gate for the PLAN_ONLY code path, which is exactly the untested combination this round introduced (only tests/test_orchestrator_runtime.py::test_multi_slice_plan_binding_pins_original_approved_commit_not_slice_start and tests/test_artifact_migration.py's new legacy-state test were added; no PLAN_ONLY-path regression test was run before this diff was submitted). This is a genuine, orchestrator-confirmed availability regression in the PLAN_ONLY internal validation/handoff-repair/resume flow, not a pre-existing failure — it must be fixed in the scope of this same correction Slice; it must not merge red, and no red-state follow-up Slice is authorized for this fingerprint. | BLOCKER | angenommen | erledigt: Reconfirmed fixed in current diff against original closure rationale; no regression found on re-derivation. |
| C-19 | claude | &#96;resolve_resume_state()&#96; in &#96;src/artifact_migration.py&#96; verifies the workflow-completion fact only in the chain→mirror direction: &#96;completions = [item for item in chain if isinstance(item.payload, WorkflowCompletionPayload)]; if completions: ...&#96; — when &#96;completions&#96; is empty, the block is skipped, so no check exists for the mirror→chain direction of the last and most consequential record type. If a restored/legacy &#96;state.json&#96; reports &#96;current_step is WorkflowStep.COMPLETED&#96; with every slice's &#96;commit_ref&#96; set, but the paired record chain contains zero &#96;WorkflowCompletionPayload&#96; records (e.g. an operator restores a state mirror and record chain from mismatched backups, or a chain directory is truncated/replaced after completion), &#96;resolve_resume_state()&#96; silently accepts the run as a valid structured-v1 resume instead of halting with &#96;ArtifactResumeError&#96;. This is exactly the fail-open "chain-empty ⇒ check skipped" pattern already fixed as BLOCKER for gate decisions (C-05) and quota pauses (C-06), and is structurally identical to the symmetric-set pattern (&#96;set(x) != set(y)&#96;) used everywhere else in this function (task, attestations, quota, commit bindings) — but was never applied to workflow completion itself. &#96;tests/test_artifact_migration.py&#96; has no test constructing a &#96;state.json&#96; at &#96;WorkflowStep.COMPLETED&#96; with all slices committed but zero chain &#96;WorkflowCompletionPayload&#96; records, so the gap is unguarded. | BLOCKER | angenommen | erledigt: Reconfirmed fixed in current diff against original closure rationale; no regression found on re-derivation. |
| C-20 | claude | The bound orchestrator attestation for fingerprint e7057975a601260382edcea4f8f599cc3d99d511fafad069af0e72fb8df45807 is FAIL: &#96;resolve_resume_state()&#96;'s new unconditional &#96;if completed and not completions: raise mismatch("state-v3 mirror reports workflow completion without a structured record")&#96; guard (added in this same correction round to close C-19) now rejects resume for four previously-passing, untouched production-flow tests in &#96;tests/test_orchestrator_runtime.py&#96; (&#96;test_inbox_workflow_creates_slice_audit_at_implementation_start_and_finalizes_overall&#96;, &#96;test_production_session_plans_commits_two_slices_and_persists_final_state&#96;, &#96;test_not_ready_gate_resumes_with_plain_resume_without_explicit_approval&#96;, &#96;test_generated_implementation_handoff_skips_second_plan_review&#96;). These exercise ordinary structured-v1 plan/commit/gate/resume sequences where the mirror can legitimately reach &#96;WorkflowStep.COMPLETED&#96; with every slice's &#96;commit_ref&#96; set at a point that precedes or is decoupled from the actual &#96;WorkflowCompletionPayload&#96; chain append, so the new guard cannot distinguish this legitimate in-flight production ordering from the genuinely corrupted/mismatched-restore mirror state C-19 targets — the same reader-gate-too-broad failure pattern previously seen and fixed in C-17/C-18 for the plan-binding fact, now reintroduced for the completion fact. This must be fixed within the current correction Slice's own scope (&#96;src/artifact_migration.py&#96;, and &#96;src/orchestrator.py&#96;/&#96;src/workflow.py&#96; if the actual defect is completion-record persist ordering rather than the resume-check itself, all already in this Slice's allowlist); it must not merge while the bound attestation is red, and no red-state follow-up Slice is authorized for this fingerprint. | BLOCKER | angenommen | erledigt: Reconfirmed fixed in current diff against original closure rationale; no regression found on re-derivation. |
| C-21 | claude | &#96;resolve_resume_state()&#96;'s commit-binding check in &#96;src/artifact_migration.py&#96; (and the analogous plan_commit/implementation_handoff paths reachable from &#96;persist_implementation_handoff&#96;/&#96;commit_slice&#96; in &#96;src/orchestrator.py&#96;) verifies only that &#96;BindingPayload.target&#96; is a member of &#96;state.slices[*].commit_ref&#96; — it never verifies that &#96;BindingPayload.attestation_id&#96; resolves to an actually-persisted &#96;ValidationAttestationPayload&#96; record sharing the binding's fingerprint, nor that every entry of &#96;BindingPayload.approval_ids&#96; resolves to an actually-persisted &#96;ReviewPayload&#96; record with &#96;verdict == "approved"&#96; and the same fingerprint. &#96;BindingPayload.__post_init__&#96; in &#96;src/artifact_models.py&#96; only calls &#96;_require_identifier&#96;/&#96;_require_unique_identifiers&#96; (generic string-shape checks); &#96;ArtifactStore._validate_store_invariants&#96; performs no cross-record existence/type check either. &#96;assert_structured_decision_context()&#96; in &#96;src/orchestrator.py&#96; reconciles &#96;ReviewPayload&#96; records against the state-v3 mirror as an unordered &#96;Counter&#96; over the whole run, not scoped to the specific attestation/approval set a given commit binding actually cites. This is exactly the "fail-open chain-empty/unreferenced ⇒ silently accepted" pattern already found and fixed as BLOCKER in this same branch for gate decisions (C-05), quota pauses (C-06), the approved-plan binding (C-16/C-17), and workflow completion (C-19) — but it was never extended to the commit/plan_commit/implementation_handoff &#96;BindingPayload&#96; records themselves, which are the terminal authorization artifact resume relies on to prove a committed Slice or handoff was genuinely reviewed and validated. A restored/corrupted record directory (mismatched backup restore, truncated/hand-edited records) containing a schema-valid commit &#96;BindingPayload&#96; whose &#96;attestation_id&#96;/&#96;approval_ids&#96; point to nonexistent or unrelated records is therefore accepted as a valid &#96;structured-v1&#96; resume instead of halting with &#96;ArtifactResumeError&#96;, directly contradicting the repository's fail-closed resume contract ("Missing, corrupt, unknown, or mirror-divergent structured records require restoring the matching chain or mirror before continuation"). No test in &#96;tests/test_artifact_migration.py&#96; constructs a chain whose commit &#96;BindingPayload.attestation_id&#96; or &#96;approval_ids&#96; do not resolve to real, fingerprint-matching records; this defect is unguarded by any test across the 11 correction rounds reviewed above, which otherwise systematically closed every other instance of this exact asymmetry class. | BLOCKER | angenommen | erledigt: Symmetric attestation/approval referential-integrity check for BindingPayload verified present and covered by the exact required parametrized regression test; provenance can no longer be fabricated across a restored/corrupted record chain. |
| C-22 | claude | src/orchestrator.py's &#96;_recover_completed_reviewer_contract()&#96; had its &#96;AgentFailureKind.AUTH&#96;-only gate removed in this correction round (unrelated to and unrequired by C-21), so it now recovers a "complete reviewer contract" from ANY misclassified &#96;AgentInvocationError&#96;, including &#96;AgentFailureKind.QUOTA&#96; — the exact classification that must otherwise route through the quota-pause/resume machinery this branch already hardened (C-06/C-14) to guarantee a matching &#96;QuotaPausePayload&#96; chain record for every automatic quota wait. No test in this diff proves that a QUOTA-classified failure whose &#96;provider_text&#96; parses as a complete contract still results in the correct quota-pause record being persisted (or is intentionally exempt with a documented rationale); as written, such an event is silently treated as a successful review, bypassing the provider-quota wait window and producing an audit trail that misrepresents what actually happened. This is an unrequested, undocumented scope widening inside a correction-convergence round and reopens the exact bypass class C-06/C-14 were raised to close, for a new, untested code path. | BLOCKER | angenommen | erledigt: The reported scope-widening bug (AUTH-only recovery gate silently accepting QUOTA-classified failures) is closed by direct test evidence under the bound fingerprint: (1) &#96;test_reviewer_does_not_recover_real_incomplete_or_foreign_failure&#96;'s new QUOTA-classified cases — including one whose &#96;provider_text&#96; is a fully valid, complete reviewer contract (&#96;SLICE_APPROVAL: 11 &#124; YES&#96; / &#96;STATUS: DONE&#96;) — assert &#96;caught.value is failure&#96;, i.e. the original AgentInvocationError propagates unrecovered instead of being silently treated as a successful review; (2) &#96;test_quota_classified_recoverable_contract_still_persists_matching_quota_pause_record&#96; drives the real &#96;WorkflowEngine._invoke_role&#96; path with a QUOTA error carrying a complete-looking contract and asserts &#96;calls == 2&#96; (the agent was actually re-invoked rather than short-circuited by recovery), &#96;invocation_failures[-1].automatic_resume is True&#96;, &#96;failure_kind is AgentFailureKind.QUOTA&#96;, and exactly one matching &#96;QuotaPausePayload&#96; chain record with the correct role/retry_at. This is precisely the C-06/C-14 bypass-class guarantee C-22 flagged as reopened, and it is now proven end-to-end (wait → retry → matching chain record), not just at the classification-gate level. I could not see a direct diff hunk for &#96;src/orchestrator.py&#96;'s &#96;_recover_completed_reviewer_contract()&#96; in the supplied packet (only artifact_migration.py/tests/docs hunks were shown), but the acceptance test named in my own finding is the exact test that now passes under the bound attestation, and the negative parametrized cases independently corroborate the behavior at the unit level, so I treat this as sufficient closure evidence per the finding's own acceptance criterion. |
<!-- audit:decision-table:end -->

## Rückdokumentation in die Arbeitsplan-MD

Arbeitsplan: `docs/internal/strukturierte-agentenkommunikation-implement-review-542ccc72.md`

## Freigabestatus

<!-- audit:approval-status:begin -->
- Implementierung bereit: `YES`
- Validierung: `PASS`
- Claude-Freigabe: `YES`
- Antigravity-Freigabe: `YES`
- Red-State-Folgeslice: `NONE`
- Commit autorisiert: `YES`
<!-- audit:approval-status:end -->
