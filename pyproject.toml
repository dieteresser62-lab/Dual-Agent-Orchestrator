[project]
name = "dual-agent-orchestrator"
version = "0.1.0"
description = "Portable dual-agent coding workflow orchestrator."
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
markers = [
    "crash_harness: full provider-free crash-injection and resume convergence proof",
]
