[project]
name = "dual-agent-orchestrator"
version = "0.1.0"
description = "Two-phase dual-agent coding workflow orchestrator."
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
